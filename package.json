{
  "name": "textgames",
  "version": "1.0.0",
  "description": "Some text display utils",
  "main": "index.js",
  "scripts": {
    "serve": "live-server . --host=localhost --port=8080",
    "compress": "html-minifier-terser index_dist.html -o dist/index.html  --collapse-whitespace --remove-comments --remove-redundant-attributes --remove-empty-attributes --remove-optional-tags --minify-js true --minify-css true&&terser ./js/*.js  --compress --mangle -o ./dist/index.js&&lightningcss --minify ./style.css -o dist/style.min.css",
    "zip": "cd dist && bestzip ../release/final.zip *"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ElementalSystems/TextGames.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bestzip": "^2.2.1",
    "html-minifier-terser": "^7.2.0",
    "lightningcss-cli": "^1.33.0",
    "live-server": "^1.2.0",
    "terser": "^5.44.0"
  }
}
